<h1>Register</h1>
<form action="/register" method="post">
    <div id='errorMsg' style="color:red; text-align: center">
        <%= locals.error %>
    </div>
    <label>Email:
        <input type="email" name="email" value=<%=locals.email%>>
    </label>
    <br />
    <label>Password:
        <input type="password" name="password" id="password" value=<%=locals.password%>>
        <button type="button" onclick="{
            if (document.getElementById('password').type === 'password') {
                document.getElementById('password').type = 'text';
            } else {
                document.getElementById('password').type = 'password';
            }
        }">👁</button>
    </label>
    <br />
    <label>Confirm Password:
        <input type="password" name="confirmPassword" id="confirmPassword" value=<%=locals.confirm%>>
        <button type="button" onclick="{
            if (document.getElementById('confirmPassword').type === 'password') {
                document.getElementById('confirmPassword').type = 'text';
            } else {
                document.getElementById('confirmPassword').type = 'password';
            }
        }">👁</button>
    </label>
    <br />
    <button type="submit">Submit</button>
</form>
<br />
<div id="button-container">
    <a href="/">Home Page</a>
</div>